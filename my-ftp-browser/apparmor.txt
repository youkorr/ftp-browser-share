profile ftp-browser-share {
  # Autoriser l'exécution des binaires de base
  /bin/** ix,
  /sbin/** ix,
  /usr/bin/** ix,
  /usr/sbin/** ix,

  # Autoriser Python
  /usr/bin/python3 ix,
  /usr/lib/python3*/** r,

  # Autoriser Nginx
  /usr/sbin/nginx ix,

  # Accès aux répertoires
  /config/** rw,
  /data/** rw,
  /tmp/** rw,

  # Accès réseau
  network inet tcp,
  network inet udp,

  # Autoriser l'exécution des scripts
  /run.sh ix,
  /etc/services.d/** ix,
  /etc/cont-init.d/** ix,
}
